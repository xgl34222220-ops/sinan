# 天机 v5.7.0 云端持续运行版

## 全天后台运行
- 新增可独立部署的天机云端服务：即使手机关闭、断网或被系统清理，服务器仍持续同步开奖、生成下一目标期预测并等待开奖验证。
- 同时运行确定性云端本机模型与可选外部 AI；未配置 AI Key 时仍保留本机云端预测。
- 每条结果按彩种、目标期、来源、模型和名次唯一冻结，容器重启不会重复生成或覆盖首次结果。

## Android 云端与本地双保险
- App 自动读取 `https://tianji-xgl.duckdns.org` 的后台预测档案，并与手机本地 AI 档案合并展示。
- 云端连接只有 3 秒连接、5 秒读取上限；服务器到期或故障时静默降级，不阻塞首页。
- 手机原有开奖直连、用户 API Key 直连 AI、本机统计预测、本地 SQLite 学习与验证全部保留，不依赖 VPS 才能打开或预测。

## 部署与安全
- 新增 Docker Compose、Caddy 自动 HTTPS、FastAPI、SQLite WAL、后台 Worker、健康检查和每日一致性备份。
- 新增一条命令交互式安装脚本；AI Key 只在 VPS 终端录入并写入权限为 600 的 `.env`，不会提交 GitHub。
- 公开接口仅提供只读开奖记录与预测；强制执行后台任务需要随机管理令牌。

> 随机开奖不可可靠预测。本版本解决的是持续运行、首次冻结、真实目标期验证和云端/本地容灾，不承诺盈利、必中或固定准确率。
