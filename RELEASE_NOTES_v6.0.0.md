# 天机 v6.0.0

## AI 对话与预测链路重构

- 恢复自然对话：当前一句问什么就回答什么，普通聊天不再被强行带入预测流程。
- 同一会话可自由切换第一名到第十名；当前问题指定的名次拥有最高优先级，旧名次只保留在聊天记录中，不再污染本轮分析。
- 连续性改为按期号判断，而不是按名次永久继承：隔几天重新询问同一名次，会直接基于当前 API 数据开始新的分析。
- 只有紧邻当前目标期的已结算预测，或用户明确要求复盘时，才带入对应历史候选、实际号码和命中结果。
- “不中，再看看下一期第一名”等自然表达会同时完成真实复盘与下一期分析，不再机械重抄上一期候选。
- 澳洲幸运10与幸运飞艇严格按当前彩种隔离，澳洲幸运10使用 `lotCode=10012` 的当前上游开奖 API 数据。
- 最近10期序列、20/60/120期次数、遗漏和目标期均由程序从本次 API 响应逐期计算；AI只能基于这些事实解释、比较和预测，不能编造另一套数字。
- 用户只问近十期就直接回答近十期，只问分析就只分析；不再强塞固定报告、默认六码或无关统计。
- 修复预测目标期与顶部目标期不一致、旧期号复制、不同名次上下文混用等问题。
- 对话历史新增名次作用域，并兼容旧版本存档。

## 质量验证

- 增加普通聊天、自然候选请求、名次切换、跨期新分析、相邻期未中复盘、显式历史复盘、真实 API 序列与统计校验等回归测试。
- 通过 Android 单元测试、Lint、Debug 与 Release 构建。
- 保持原包名与正式签名，可直接覆盖升级。

> 随机开奖结果无法可靠预测。本功能用于统计实验、记录与真实前向验证，不承诺准确率或收益。
