# 天机 v6.1.1

这是天机 6.1 系列的正式稳定更新，重点修复 Telegram 重复预警，并进一步整理 App 首页、通知跳转和云端控制台。

## Telegram 与系统推送

- 修复两个彩种并行 Worker 同时读取同一待发送事件时，Telegram 可能连续推送两条完全相同消息的问题。
- 推送前改为 SQLite 原子抢占；同一事件和同一接收方只能由一个线程发送。
- 每个彩种只处理自身待发送事件，减少跨彩种重复扫描。
- 增加 8 线程并发回归测试。
- 预测完成通知点击后直接进入对应彩种预测首页；风险预警仍进入通知中心。

## App 界面与交互

- 删除首页重复的目标期摘要条，把正式预测结果提升到第一屏。
- 手机首页顺序调整为：彩种、本期预测、最新开奖、分析/概率/模型。
- 600dp 及以上设备启用双栏自适应布局。
- 修复把 AI 接口“已连接”误显示成“预测已完成”的统计逻辑。
- AI 状态改为“可用、正在、本期结果、失败”，不再展示含混的内部路数。
- 将“影子实验、证据闸门、前向证据”等默认文案改为普通用户可理解的表达。
- 关键页面最小字号提升至 11sp，改善字体放大后的可读性。
- 底部导航恢复轻量触摸反馈和振动反馈。
- 删除旧首页和旧通知中心入口，避免新旧 UI 同时维护。

## 云端控制台

- 增加 AI 任务、FCM 和 Telegram 最近 24 小时健康统计。
- 状态刷新失败时明确显示异常和最后成功刷新时间，不再静默保留旧数据。
- 继续显示运行 Commit、部署目标、容器启动、数据库、Worker、备份和回滚状态。

## 仓库与质量保障

- README、Android 版本号和正式发行说明加入一致性检查。
- 新增 UI 文案契约，防止“1 路 AI 已完成”等误导文案回归。
- 根目录只保留最近三个正式版本的发行说明，删除旧 Beta 与历史残留。
- Android 单测、Lint、Debug/Release 构建及服务端单测、Compose、Docker 构建全部作为发布门禁。

> 随机开奖不可可靠预测。本项目用于统计实验、记录与前向验证，不承诺收益、准确率或必中。