# 天机 v6.9.0

v6.9.0 聚焦 **Android AI 服务配置中心、OpenAI 兼容中转接入与单一正式 AI 调用保护**。本版本允许保留多个 AI 配置，但主页正式预测始终只调用当前选中的一个 AI，避免多个 API Key 同时产生请求和费用。

## AI 服务配置中心

- 设置页统一管理 OpenAI、DeepSeek 与自定义 / 中转接口。
- 支持填写自定义 HTTPS API Base URL，并按接口能力选择 Chat Completions 或 Responses 路由。
- 支持任意兼容模型 ID，不再限制为内置模型名；第三方网关的 `free` 等路由可直接作为模型名保存。
- 支持读取模型列表、测试连接、历史窗口、推理强度与接口能力状态。
- API Key 继续使用本机安全配置存储，不写入公开仓库、Release 附件或构建日志。

## 当前 AI 与费用保护

- 可以同时保存多个 AI 配置，但新增唯一“当前 AI”状态。
- 主页“生成本期 AI 动态预测”只解析并调用当前 AI，不再在未指定 profile 时遍历全部完整配置。
- 当前 AI 选择会持久保存，App 重启后继续使用上次选择。
- 设置页每张 AI 卡片新增“当前 AI / 设为当前”；切换只修改本机选择，不发送 API 请求。
- 单独点击某个 AI 的“立即分析”时只调用该配置，并将其设为新的当前 AI。
- 删除当前 AI 后自动回退到下一个完整配置；没有完整配置时停止正式 AI 调用。
- 保留已有并发配置作为单个 AI 的任务并行能力，不再将并发数解释为同时调用多个独立 Key。
- 同一目标期已有冻结预测时继续拒绝重复计费调用。

## 兼容性与稳定性

- 本版本为 Android 侧功能升级，不修改 FastAPI 服务端预测引擎、开奖同步、Dynamic AI v2 云端计算、FCM / Telegram 链路和历史结算语义。
- 保持正式应用 ID `com.tianji.probabilitylab.nativev5` 与既有正式签名证书，可直接覆盖升级 v6.8.1。
- 正式版 versionCode 升级为 75，versionName 为 6.9.0。
- Android CI 继续执行契约校验、单元测试、Lint、Debug / Release 构建与 Compose 截图基线。

> 天机用于统计实验、记录与真实前向验证。随机开奖不可可靠预测，本项目不承诺准确率、收益或必中。第三方 AI 网关的可用性、额度与计费规则由对应服务商决定。
