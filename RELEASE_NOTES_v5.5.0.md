# 天机 v5.5.0 稳定重构版

- 真正取消运行中和排队中的 AI 任务，避免刷新、切换彩种后继续产生无效调用。
- AI 返回后重新核验最新开奖、目标期与服务器截止时间，跨期开奖结果不进入前向档案。
- 强制同步后和发送请求前再次检查已有冻结记录，避免目标期变化造成重复计费。
- 新增同期开奖冲突检测，历史接口和最新接口数据不一致时立即停止预测。
- 预测缓存改为完整历史内容 SHA-256 指纹，上游修正历史后自动重新训练。
- 按彩种设置历史目标并分别计算数据充分度。
- 拆分 AI 任务注册、开奖合并、目标期守卫、历史指纹和版本化预测入口。
- 新增排队任务取消、跨期开奖、目标期变化、数据冲突和缓存失效测试。
- 保留系统字体缩放，不再覆盖用户的无障碍设置。
- 发布流程统一为 CI 与标签发布，签名材料从 GitHub Environment Secrets 恢复。

> 随机开奖不可可靠预测。本项目用于统计实验、记录和真实前向验证，不承诺准确率或收益。
