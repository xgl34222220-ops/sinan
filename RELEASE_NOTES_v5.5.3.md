# 天机 v5.5.3 模型上限与透明计算

- 官方 DeepSeek V4 请求使用模型自身 384K 最大输出空间，不再由客户端写死 32K、65K 或 96K 上限。
- OpenAI 与未知兼容模型不发送客户端输出 Token 上限，由所选模型和供应商执行自身限制。
- Chat Completions 使用流式接收，页面实时显示连接、思考和生成结构化结果的阶段与耗时。
- 不支持流式参数的兼容接口会自动退回普通对话响应。
- AI 返回结果新增可核验计算摘要、名次依据、候选依据和不确定性说明。
- 不展示或保存模型隐藏思维链，只展示基于已核验统计的简洁依据摘要。
- 修正“重试结果关闭推理”的错误提示；重试仍保留真实思考。

> 最大输出空间不会强迫模型生成到上限，模型仍会在完成任务后自然停止。深入推理的总耗时仍取决于模型、网络和供应商负载，流式状态用于避免长时间黑箱等待。
