# 天机 v6.1.0-beta03

本次预发行版集中统一 App 布局、通知中心、系统推送、Telegram 展示和云端部署状态，重点解决悬浮底栏遮挡、普通预测误用高优先级通知频道，以及服务器更新状态难以核验的问题。

## App 界面与交互

- 首页新增目标期、预测名次、最终六码和 AI 状态焦点卡。
- 新增 AI 独立评审进度浮层，展示运行中、已完成和失败任务。
- 悬浮底栏改由统一 Scaffold 管理，正确处理系统安全区域。
- 首页、策略、档案和设置页面移除重复手写的大块底部占位，统一使用正常内容间距。
- 通知中心支持按未读、预测、预警、恢复、彩种和来源筛选。
- 通知记录改为紧凑可展开卡片，并可在同页调整接收范围。

## Android 通知与 FCM

- FCM 改为 data-only，由 App 根据事件类型选择通知频道并统一渲染。
- 普通预测与风险预警使用不同频道和提醒强度，普通预测不再误触高优先级警报。
- 系统通知按彩种归组。
- 新增“查看预测”和“标记已读”快捷操作。

## Telegram 推送

- 预测与预警消息改为更紧凑的卡片结构。
- 配置公开地址时可显示“查看最新预测”和“打开管理面板”按钮。
- 保留仅推送云端 AI、忽略云端本地模型的既有来源规则。
- 保留“提前预警、加强提醒、升级提醒”等稳定标题，兼容现有测试和识别逻辑。

## 云端控制台与部署状态

- 显示当前运行 Commit、目标 Commit、部署结论和容器启动时间。
- 明确显示自动更新成功、检查失败、更新阻塞、回滚中、已回滚和回滚失败。
- 手动更新脚本同步写入部署状态，自动和手动更新结果均可在控制台核验。

## 验证

- Android 单元测试、Lint、Debug APK 和 Release APK 构建通过。
- 服务端部署脚本验证、Python 编译、74 项单元测试、Docker Compose 校验和 Docker 镜像构建通过。
- 未修改服务端预测算法、AI Key、管理密码或数据库内容。

> 随机开奖不可可靠预测。本项目用于统计实验、记录与真实前向验证，不承诺准确率、收益或必中。
