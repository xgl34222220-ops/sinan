# 天机 v6.1.2

本次更新把服务端与 App 端的 AI 预测统一升级为可验证的持续在线学习系统。系统只根据已经开奖并完成结算的前向结果更新权重，不再把每一期临时生成的语言模型判断当成“学习”。

## 持续学习预测

- 第 1 至第 10 名分别建立滚动前向验证档案，独立统计六码命中率、Log Loss、边界差和最长连续未中。
- 频率、遗漏、状态转移、趋势、稳定性、012 路、奇偶、大小和万能六码结构分别结算；长期无效的策略自动降权。
- AI 名次判断接入真实外样本证据门槛。存在明显更强名次时，弱名次不能只靠一次极端 AI 评分直接获胜。
- AI 号码评审按彩种、AI 配置、模型模式和具体名次分别保存学习权重，避免不同名次互相污染。
- App 端正式预测增加十名次滚动回测、弱名次否决和号码矩阵校准；远程 AI 仍只读取原始历史，不读取本地模型最终六码。

## 独立性与统计修复

- 增加同目标期 AI 与本地六码碰撞审计；高度重合时执行隐藏近期数据的独立留出复核。
- 留出复核后仍与本地六码完全一致且无法证明独立性时，不写入正式 AI 档案。
- 修复“连续不中”跨彩种、来源和模型混算的问题，并按目标期去重。
- 20、50、100 期和全部窗口分别计算自己的连续不中数据，不再重复显示同一个全局值。

## 说明

持续学习表示系统会随真实结算结果更新、降权和淘汰无效策略，不代表随机开奖能够被稳定预测，也不保证准确率单调提升或盈利。