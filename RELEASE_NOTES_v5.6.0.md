# 天机 v5.6.0 持续学习与独立 AI

## AI 不再照抄本机答案
- 分析对话默认使用“独立学习”：不向外部 AI 发送本机已选名次和六码。
- 新增“参考本机”和“反向审计”，只有用户明确选择时才允许读取本机候选。
- 每次候选显示所用判断模式，避免把解释型回答误认为独立预测。

## 可验证的持续学习
- 新增本机持久化在线学习引擎，按 AI 配置、模型、彩种和名次分别积累真实前向结果。
- 使用贝叶斯频率、指数衰减热度、遗漏、收缩转移、状态变化和跨窗稳定性六类独立信号。
- 开奖后根据实际号码逐期更新因子权重；未中会降低失效因子、提高更能解释实际号码的因子。
- 下一次 AI 请求会收到已结算期数、命中率、连续未中、权重变化和当前独立信号，要求主动改变策略而不是机械复刻上一期。
- 正式 AI 预测与分析对话共用同一套可审计学习档案，但各模型配置分别学习。

## 正式预测可靠性
- 修复 DeepSeek 首轮只返回思考内容时，错误地把 reasoning_content 当作可继续对话状态的问题。
- 首轮没有完整 position + scores 时，改为同模型关闭额外思考并重新发送精简结构化任务，最多 20 秒收口。
- 不同思考强度使用不同墙钟预算，完整预测核心到达仍立即冻结。

## 新分析体系
- 新增自适应学习、贝叶斯大数据、状态走势和万能码共识四种分析人设。
- “万能码”只表示多个独立信号的共识候选池，会显示冲突和样本风险，不承诺必中。

## 档案与界面
- 真实前向档案新增“清除显示、最近 10 条、显示全部”；清除显示只隐藏列表，不删除原始档案、统计或 SHA-256 链。
- 分析对话显示学习样本、命中率、连续未中和最近策略调整。
- 过程事件进一步压缩，减少长时间推理时卡片堆叠。

> 随机开奖不能被保证预测。本版实现的是可审计的长期学习、独立判断和真实前向复盘，不承诺盈利、必中或固定准确率。
