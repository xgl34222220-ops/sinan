# 天机 v6.3.0

v6.3.0 是一次以 UI 完整度、推送中心可用性、档案浏览效率和服务端可观测性为核心的正式体验升级。

## Android UI / 体验

- 首页预测层级重新打磨：保留实时开奖优先级，精简重复位置选择，概率榜加入“六码边界”提示。
- AI 摘要不再静默截断，超过 4 个模型时明确提示剩余模型结果并引导进入 AI 页面查看完整判断。
- 核心预测指标继续突出，训练历史降级为辅助信息，减少首页视觉噪音。
- 平板 / 宽屏布局改为统一纵向滚动的双栏内容结构，避免左右区域滚动不同步。
- Dock 与 Navigation Rail 恢复可见按压反馈，加入轻微缩放动画并保留触觉反馈。
- 统一语义色：成功 / 恢复为绿色，预警为琥珀色，严重异常为红色，AI 为紫色，避免动态取色或彩种色与状态色冲突。
- 设置页面辅助字号整体提升，同时继续遵循系统字体缩放。

## 推送中心

- 推送设置改为 Material 3 Bottom Sheet，不再长期占用顶部空间。
- 日期分组升级为粘性标题，长列表浏览更清晰。
- 筛选按钮显示当前启用筛选数量。
- 预测、预警、严重异常、恢复等消息采用更清晰的状态层级和视觉强调。
- 未读消息增加侧边强调标记。
- 通知卡片现在是真正可执行的导航入口：
  - 预测完成 → 对应彩种首页
  - 风险预警 / 恢复 → 对应彩种档案页
  - 其他系统消息保留在通知中心
- 点击未读通知时会先完成已读状态更新，再执行跳转。

## 档案

- 修复历史记录只显示前 120 条且没有提示的问题。
- 新增“已显示 X / 总数”进度信息。
- 增加“继续加载”分页加载逻辑，避免长列表一次性渲染，同时不再静默丢失记录。

## 服务端控制台

- 将原先分散的状态卡片收敛为紧凑的“运行总览”。
- 统一展示云服务、开奖链路、AI 任务、FCM 和 Telegram 健康状态。
- 健康状态默认收敛，异常状态保持更高可见性。
- 实时开奖链路新增“优秀 / 正常 / 偏慢 / 异常”延迟等级。
- 增加当前延迟与 EMA 趋势方向提示。
- 当实时数据刷新失败时，旧数据会明确标记为“数据已过期”，避免将缓存状态误判为实时状态。
- 控制台微小字号整体上调，提升移动端和桌面端可读性。

## 工程质量

- 新增完整首页、宽屏 / 平板、推送中心等 Compose 截图回归场景。
- 服务端增加 v6.3 体验契约测试，保护运行总览、延迟状态和过期数据提示。
- Android Debug / Release 单元测试、Lint、APK 构建与截图基线已通过验证。
- 服务端 Python 编译、单元测试、Docker Compose 校验及 Docker 镜像构建已通过验证。

## 安装与升级

- 包名保持 `com.tianji.probabilitylab.nativev5`。
- 使用与上一正式版相同的受保护签名证书，可直接从 v6.2.0 覆盖升级。
- 正式 Release 流程会再次校验上一版本 APK 签名、当前 APK 签名、包名、versionCode、versionName 和 SHA-256。
