# 天机 v5.5.6 完整结果立即返回

- 不降低模型 Token 上限，不关闭真实 thinking，不减少 60/120 期历史。
- 流式响应一旦取得有效名次和 10 项评分，立即主动结束剩余输出并进入本机校验。
- 不再等待供应商继续生成长篇说明或最终结束标记，避免预测结果已经生成仍持续等待。
- 修复流式空值被拼接成 `nullnull…` 并干扰 JSON 判断的问题。
- 说明未生成完时保留有效预测矩阵，使用本机逐期核验摘要，不重新请求模型。
- 主动提前结束导致供应商未返回最终 Token 用量时，不伪造 Token 数字。

> 本次修复只改变结果接收和收口时机，不压缩模型可用思考空间。
