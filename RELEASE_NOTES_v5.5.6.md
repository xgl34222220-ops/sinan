# 天机 v5.5.6 轻量核心预测与立即返回

- 恢复此前稳定的轻量结构：AI 只返回 `position` 与 10 项 `scores`，六码、七码和说明由客户端生成。
- “自动”模式恢复为供应商/模型默认思考，不再偷偷强制 DeepSeek `high` 推理。
- “深入”仍保留 DeepSeek `max` 推理；“省时”按界面含义关闭可控 DeepSeek thinking。
- 保留模型自身最大 Token 空间、真实 thinking 和完整 60/120 期历史，不使用客户端硬截断换速度。
- 流式响应一旦取得有效名次和 10 项评分，立即主动结束剩余输出并进入本机校验。
- 不再要求 AI 同时编写因素权重、计算摘要、名次说明、候选说明和风险长文，避免简单预测被扩成超长报告。
- 能力测试在自动模式下不再额外启动一次强制高推理请求。
- 修复流式空值被拼接成 `nullnull…` 并干扰 JSON 判断的问题。
- 模型已开始推理后，缺少完整核心 JSON、超时或断流都会明确失败，不再自动补全或重新预测。
- 仅当接口在模型推理开始前明确拒绝显式思考参数时，允许切换为模型默认协议兼容重发一次。

> 这次恢复的是以前能较快生成结果的核心协议，同时保留 v5.5.6 的完整结果到达后立即收口能力。
