# 天机 v5.5.6 多会话分析对话

## 跨期开奖持续对话

- 新增独立分析对话入口，同一条会话可以跨越多期开奖持续追问。
- 开奖后可以继续反馈“上一期没中，降低短期热号权重”，AI 会读取当前会话此前的问题、回答、候选和客户端核验结果。
- 每次候选绑定生成时的目标期与对应助手消息；开奖接口更新后自动补充实际号码和六码命中状态。
- 对话中的策略调整只改变后续聊天研判侧重点，不会伪装成训练，也不会修改正式预测模型参数。

## 多会话与上下文续接

- 支持新建空白对话或继承上一段策略摘要继续。
- 历史入口显示真正的会话列表，旧会话可重新打开并继续聊天。
- 接近安全上下文阈值时，先完整保存旧会话，再提取明确策略反馈、候选核验和近期讨论建立续接会话。
- 完整旧消息不会因上下文压缩而删除。

## 持久化、模型与人设

- 会话使用应用私有目录和 `AtomicFile` 原子写入，应用关闭、进程回收或手机重启后仍可恢复。
- AI 配置与具体模型分开选择；DeepSeek 默认支持 `deepseek-v4-flash` 和 `deepseek-v4-pro`，同时合并接口返回的其他可用模型。
- 内置综合研判、历史分析、走势研判、遗漏追踪、转移分析和风险审计六种人设。
- 对话存储与正式预测数据库隔离，不写入真实成绩、不覆盖首次冻结结果、不参与正式共识。

## 流式输出与排版

- 兼容 Chat Completions SSE 与 Responses API 流式事件。
- 模型开始输出正文后，消息逐段增长；停止生成时保留已显示内容。
- 接口不支持流式时自动降级普通请求并平滑分段显示。
- 支持基础 Markdown，加粗、列表、引用、分隔线和行内代码不再显示原始标记。

## 正式预测保持 v5.5.5 基线

- 外部正式 AI 预测的提示词、多因素要求、解释字段、推理阶段、同一对话补全、流式结束和结果校验均恢复并保持 v5.5.5 行为。
- v5.5.6 的聊天功能使用独立客户端和独立存储，后续聊天 UI 或会话逻辑调整不再影响正式预测。

## 自动验证

分支持续执行 Debug/Release 单元测试、Android Lint、Debug APK 和 R8 Release APK 构建。只有全部通过的构建产物用于安装测试。

> 随机开奖不可可靠预测。本项目用于统计实验、记录和真实前向验证，不承诺准确率、收益或必中。
