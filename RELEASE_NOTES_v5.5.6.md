# 天机 v5.5.6 轻量核心预测与开奖记录对话

## 具体模型选择

- “AI 配置”和“具体模型”分成两行选择，不再把“DeepSeek 主力”配置名称误当成模型。
- DeepSeek 默认可选 `deepseek-v4-flash` 与 `deepseek-v4-pro`。
- 同时合并配置中当前模型、接口拉取到的可用模型和供应商内置备用模型。
- 切换具体模型会打开该模型自己的当前目标期会话，不覆盖其他模型的聊天和候选。

## 按目标期持久保存对话和候选

- 每个“彩种 + 目标期 + AI配置 + 具体模型”建立独立归档。
- 目标期开奖后只切换到新会话；上一期对话、候选、人设和实际模型继续保留。
- 对话正文、所用人设、模型、目标期、Top6/Top7、完整概率和更新时间写入应用私有存储。
- 关闭应用、进程被系统回收或手机重启后仍可恢复。
- 顶部历史入口可查看旧目标期、旧模型、消息数量和候选状态，并以只读方式打开。
- 单独清空当前会话只删除当前“目标期 + 配置 + 模型”的归档。
- 归档使用 `AtomicFile` 原子写入，异常写入不会用半份文件覆盖上一份有效记录。
- 对话归档与正式预测数据库隔离，不写入真实成绩、不覆盖首次冻结结果、不参与正式共识。

## 真正逐步输出的分析对话

- 对话请求启用 `stream=true`，兼容 Chat Completions SSE 与 Responses API 事件流。
- 模型开始输出正文后，助手消息气泡逐段增长，不再一直显示“正在分析”后突然出现整段答案。
- 思考阶段继续显示状态；正文开始后自动跟随滚动并显示生成光标。
- 点击停止会保留已经显示的部分。
- 接口返回普通 JSON 时平滑分段显示；接口明确拒绝流式参数时自动改用普通请求。
- 流式过程中隐藏候选协议标签和结构化 JSON，最终只展示自然语言正文与独立候选卡片。

## 六种内置分析人设

- **综合研判**：综合频次、遗漏、后继转移、趋势变化和本机模型参考。
- **历史分析**：专注 20/60/120 期真实次数、占比、遗漏和样本量。
- **走势研判**：比较短中长窗口的升温、降温、震荡与稳定性。
- **遗漏追踪**：分析遗漏和近期密度，同时主动避免赌徒谬误。
- **转移分析**：分析当前号码之后的历史后继分布和有效样本量。
- **风险审计**：反向检查短窗过拟合、指标冲突、边界过小和证据不足。

## 正式预测提速与稳定性

- 正式 AI 只返回 `position` 与 10 项 `scores`，六码、七码、概率矩阵和中文核验摘要由客户端生成。
- “自动”使用供应商或模型默认思考；“深入”保留高强度推理；“省时”按界面含义关闭可控 thinking。
- 保留完整 60/120 期历史和模型自身 Token 空间。
- 流式响应取得有效名次与 10 项评分后立即结束多余输出并进入本机校验。
- 修复流式空值被拼接成 `nullnull…` 的问题。
- 普通超时、断流、空响应或无效 JSON 明确失败，不隐式启动第二轮完整预测。

## 最终自动验证

- Debug 单元测试：48 项，0 失败，0 跳过。
- Release 单元测试：48 项，0 失败，0 跳过。
- Android Lint：No Issues Found。
- Debug APK：构建成功。
- Release APK：R8 混淆与资源压缩构建成功。
- 正式源码 CI：成功。

> 随机开奖不可可靠预测。本项目用于统计实验、记录和真实前向验证，不承诺准确率、收益或必中。
