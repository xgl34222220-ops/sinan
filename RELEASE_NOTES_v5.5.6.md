# 天机 v5.5.6 多会话分析与轻量核心预测

## 像聊天应用一样管理多条对话

- 分析页不再按每个目标期强制拆分会话，而是以真正的“聊天会话”为单位保存。
- 同一条会话可以跨越多期开奖持续追问，完整保留用户问题、AI 回答、所用模型、人设和候选卡片。
- 开奖后继续在原会话中说“上一期没中，降低短期热号权重并调整策略”，模型能看到前面的对话、上期候选和客户端核验的实际结果。
- 每次候选都绑定生成时的目标期和对应助手消息；接口更新后自动补充实际号码以及六码命中或未中结果。
- 对话中的“调整策略”只改变后续聊天研判侧重点，不会伪装成模型训练，也不会修改正式本机预测器参数。

## 新建对话与上下文续接

- 顶部新增“新建对话”，可选择完全空白开始，或只继承上一段的策略摘要与复盘结论。
- 历史入口改为真正的会话列表，展示标题、模型、消息数、当前目标期、最后内容和更新时间。
- 历史会话可以重新打开并继续聊天，不再只能只读查看。
- 客户端估算当前上下文占用并在顶部显示百分比。
- 接近安全上下文阈值时，先保存完整旧会话，再提取用户明确提出的策略调整、近期候选、命中核验和最近讨论，建立一条“续”会话继续。
- 完整旧消息仍保存在历史中；发送给模型的上下文只保留安全窗口和压缩记忆，避免无限堆积导致接口失败。

## 对话数据持久化与旧记录迁移

- 会话使用应用私有目录和 `AtomicFile` 原子写入，应用关闭、进程被回收或手机重启后仍可恢复。
- 归档格式升级到 schema 2，保存会话标题、长期摘要、续接关系、多轮消息和多期候选核验。
- 旧版按目标期保存的 schema 1 记录会自动迁移为可访问的历史会话，旧单个候选也会转换为候选历史。
- 对话存储与正式预测数据库隔离，不写入真实成绩、不覆盖首次冻结结果、不参与正式共识。

## 全面对话 UI 重做

- 顶部精简为当前会话标题、上下文占用、新建对话、历史、更多和关闭，不再让标题换行并堆满大图标。
- 配置、具体模型和分析人设收进紧凑控制卡，减少首屏占用。
- 聊天记录成为页面主体，消息字号、行高和气泡间距重新调整。
- 开奖衔接、上下文续接等系统事件使用居中提示条，不与普通助手回答混在一起。
- 对话候选卡跟随对应回答显示，开奖后可直接看到实际号码和命中状态。
- 输入区改为底部悬浮圆角编辑器，保留逐步输出、停止生成和键盘发送。

## 配置、模型和分析人设

- “AI 配置”和“具体模型”分开选择，不再把“DeepSeek 主力”配置名称误当成模型。
- DeepSeek 默认可选 `deepseek-v4-flash` 与 `deepseek-v4-pro`，并合并接口实际返回的可用模型。
- 保留综合研判、历史分析、走势研判、遗漏追踪、转移分析和风险审计六种人设。
- 所有人设只使用真实开奖接口历史、客户端核验统计和本机模型参考，不承诺必中、收益或真实中奖概率。

## 真正逐步输出的分析对话

- 兼容 Chat Completions SSE 与 Responses API 事件流。
- 模型开始输出正文后，助手气泡逐段增长并自动跟随滚动。
- 点击停止保留已经显示的部分。
- 接口返回普通 JSON 时平滑分段显示；接口拒绝流式参数时自动降级普通请求。
- 流式过程中隐藏候选协议和结构化 JSON，最终只展示自然语言与候选卡片。

## 正式预测提速与稳定性

- 正式 AI 只返回 `position` 与 10 项 `scores`，六码、七码、概率矩阵和中文核验摘要由客户端生成。
- 保留完整 60/120 期历史和模型自身 Token 空间。
- 流式响应取得有效名次与 10 项评分后立即结束多余输出并进入本机校验。
- 修复流式空值拼接、结果已出仍等待、普通异常隐式重跑等问题。

## 自动验证

- Debug 单元测试：51 项，0 失败，0 跳过。
- Release 单元测试：51 项，0 失败，0 跳过。
- Android Lint：No Issues Found。
- Debug APK：构建成功。
- Release APK：R8 混淆与资源压缩构建成功。
- 新增 schema 1→2 迁移、长期策略摘要、上下文续接和候选核验测试。

> 随机开奖不可可靠预测。本项目用于统计实验、记录和真实前向验证，不承诺准确率、收益或必中。
