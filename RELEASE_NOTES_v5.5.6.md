# 天机 v5.5.6 轻量核心预测与开奖记录对话

## 真正逐步输出的分析对话

- 对话请求改为 `stream=true`，同时兼容 Chat Completions SSE 与 Responses API 事件流。
- 模型开始输出正文后，助手消息气泡会像正常聊天一样逐段增长，不再一直显示“正在分析”后突然出现整段答案。
- 模型思考阶段继续显示状态；正文开始后自动跟随滚动，并用光标提示仍在生成。
- 点击停止会保留已经显示的部分，不会把已生成内容一起清空。
- 接口返回普通 JSON 时使用平滑分段显示；接口明确拒绝流式参数时自动改用普通请求，不影响兼容模型使用。
- 流式过程中隐藏候选协议标签和结构化 JSON，最终只展示自然语言正文与独立候选卡片。

## 配置与具体模型分开选择

- 顶部“配置”只负责 API 地址、密钥和供应商设置；新增独立的“模型”选择栏，不再把“DeepSeek 主力”误当成具体模型。
- DeepSeek 默认提供 `deepseek-v4-flash` 与 `deepseek-v4-pro` 两个可选模型。
- 同时合并当前配置模型、接口拉取到的可用模型和供应商内置备用模型；接口模型列表更新后，对话页可直接选择。
- 切换具体模型会进入该模型自己的当前期会话，不会覆盖其他模型已经生成的聊天和候选。
- 每条历史归档保存实际配置名称与具体模型名称，便于开奖后核对当时使用的模型。

## 按目标期持久保存对话和候选

- 每个“彩种 + 目标期 + AI配置 + 具体模型”建立独立归档，目标期开奖后只切换到新会话，不再删除上一期内容。
- 对话正文、所用人设、具体模型、目标期、候选六码、完整概率和更新时间都会写入应用私有存储。
- 关闭应用、进程被系统回收或手机重启后，历史对话和候选卡片仍可恢复。
- 顶部新增历史入口，可查看旧目标期、旧模型、消息数量和是否生成候选，并以只读方式打开。
- 打开历史后可一键返回当前目标期；历史记录不会被新问题改写。
- 单独清空当前会话只清除当前“目标期 + 配置 + 模型”的归档，不影响其他期或其他模型。
- 对话归档与正式预测数据库保持隔离，不会写入真实成绩、覆盖首次冻结结果或参与正式共识。

## 六种内置分析人设

- **综合研判**：综合频次、遗漏、后继转移、趋势变化和本机模型参考。
- **历史分析**：专注 20/60/120 期真实次数、占比、遗漏和样本量。
- **走势研判**：比较短中长窗口的升温、降温、震荡与稳定性。
- **遗漏追踪**：分析遗漏和近期密度，同时主动避免“久未出现就该出”的赌徒谬误。
- **转移分析**：分析当前号码之后历史上更常出现的后继号码和有效样本量。
- **风险审计**：反向检查短窗过拟合、指标冲突、边界过小和证据不足。
- 每个人设内置对应的快捷问题；切换人设不会改变接口历史，也不会赋予模型虚构事实的权限。

## 可连续追问的真实历史分析

- 首页悬浮入口可直接询问某个名次最近 20/60/120 期出现次数、当前遗漏、后继转移、趋势变化和相对候选。
- 支持连续追问，例如“分析第一名最近60期”“当前号码之后常接哪些号”“哪几个号下期相对活跃”。
- 每轮对话都携带当前开奖接口快照、逐期核验统计和本机模型参考，不使用虚构的本机事实记录。
- 用户要求候选时可生成独立的六码候选卡片；对话候选不会写入真实成绩、不会替换首次冻结预测，也不会参与多模型共识。
- 对话窗口支持取消请求、清空当前会话、刷新接口历史，并明确显示当前目标期和载入历史数量。
- API Key 继续使用现有 Android Keystore 加密配置；对话请求不保存隐藏思维链。
- 对“几率大”的回答明确解释为历史相对频次或模型相对评分，不冒充真实中奖概率。

## 正式预测提速与稳定性

- 恢复稳定的轻量结构：正式 AI 只返回 `position` 与 10 项 `scores`，六码、七码和说明由客户端生成。
- “自动”模式恢复为供应商/模型默认思考，不再偷偷强制 DeepSeek `high` 推理。
- “深入”仍保留 DeepSeek `max` 推理；“省时”按界面含义关闭可控 DeepSeek thinking。
- 保留模型自身最大 Token 空间、真实 thinking 和完整 60/120 期历史，不使用客户端硬截断换速度。
- 流式响应一旦取得有效名次和 10 项评分，立即主动结束剩余输出并进入本机校验。
- 不再要求 AI 同时编写因素权重、计算摘要、名次说明、候选说明和风险长文，避免简单预测被扩成超长报告。
- 能力测试在自动模式下不再额外启动一次强制高推理请求。
- 修复流式空值被拼接成 `nullnull…` 并干扰 JSON 判断的问题。
- 正常情况下仍是一轮预测；若模型只持续输出思考且迟迟不开始 JSON，在达到真实思考收口条件后保存上下文，并在同一对话仅整理核心 JSON，不重新分析历史。
- 普通超时、断流、空响应或无效 JSON 仍明确失败，不会重新开始一轮正式预测。
- 仅当接口在模型推理开始前明确拒绝显式思考参数时，允许切换为模型默认协议兼容重发一次。

## 工程保护

- 对话控制器、网络客户端、统计上下文、持久化归档和全屏对话 UI 独立于 `AppController` 与正式预测档案，降低新增功能对目标期冻结逻辑的影响。
- 归档使用应用私有目录与原子写入，异常写入不会用半份文件覆盖上一份有效记录。
- 新增归档身份、JSON 往返、候选恢复、人设完整性、流式正文隐藏、统计窗口、遗漏、后继转移和对话历史边界测试。
- CI 继续执行 Debug/Release 单元测试、Lint、Debug APK 与 R8 Release APK 构建。

> 随机开奖不可可靠预测。本项目用于统计实验、记录和真实前向验证，不承诺准确率、收益或必中。
