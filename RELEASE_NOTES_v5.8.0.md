# 天机 v5.8.0

本版本集中优化 Android App 与云端管理控制台的 UI、可读性和中文体验，不改变预测算法、目标期结算、数据库结构、接口字段或后台调度逻辑。

## Android App

- 全面统一 MIUIx 玻璃视觉层级，重做顶栏、彩种切换器、内容卡片、指标块、开奖球、空状态和悬浮底栏。
- 提升基础字号、点击区域和页面留白，减少过重阴影，改善信息密集页面的阅读体验。
- Monet 动态取色会同步影响页面背景、卡片、顶栏和底栏材质。
- 页面分区标题统一使用中文，不再混用 `NATIVE AI LAB`、`PROBABILITY MATRIX` 等英文标题。
- 云端历史档案中的英文分析和风险提示在展示时自动中文化；无法可靠转换时显示中文兜底说明。
- 保留原五个页面、AI 对话入口和全部现有业务行为。

## 云端与服务端

- 重新设计桌面端和手机端管理控制台，统一顶部栏、侧栏、筛选器、摘要、档案、模型、开奖任务和运维卡片。
- 优化手机端悬浮底栏、双列摘要、单列内容和超窄屏布局。
- 新生成的云端 AI `analysis` 与 `risk_note` 强制使用简体中文。
- 旧英文档案通过 API 和控制台读取时自动中文化，不会改写数据库中的原始记录。
- 保持服务端预测、冻结、结算和任务调度逻辑不变。

## 构建与验证

- Android Debug/Release 单元测试通过。
- Android Lint 通过。
- Debug APK 与 R8 Release APK 构建通过。
- 服务端 Python 编译和全部单元测试通过。
- Docker Compose 校验和服务端镜像构建通过。
- Release APK 使用项目既有签名证书构建，并在发布流程中校验 APK 签名、包名、版本号和 SHA-256。

> 随机开奖不可可靠预测。天机用于统计实验、记录和真实前向验证，不承诺准确率、收益或必中。
