# 天机 v5.5.4 流式结果恢复

- 修复流式模型完成大段推理后，因最终 JSON 前后包含说明、代码围栏或分段内容而被误判失败的问题。
- 只要 `position` 与 10 项 `scores` 已完整生成，即使后续可选说明被截断，也直接恢复并采用核心预测。
- 第一轮确实没有最终 JSON 时，沿用同一对话与已完成的 DeepSeek `reasoning_content` 补全结构化结果，不重新分析 120 期历史。
- 同一对话补全仍失败时停止并明确提示，不再自动启动第三次完整推理。
- 流式状态明确显示“已收到完整预测核心”“同一对话补全结果”和真正的重新请求。
- 不改变 v5.5.3 的模型 Token 上限、thinking 强度和完整历史窗口。
