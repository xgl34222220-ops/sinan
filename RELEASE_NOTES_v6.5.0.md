# 天机 v6.5.0

v6.5.0 是一次以实机体验为核心的 UI / UX 精修版本，重点统一 Android 与服务端控制台的视觉层级、移动端密度和交互语义。本版本不修改预测算法、开奖数据协议或推送业务语义。

## Android UI / UX

- 降低普通页面过重的蓝色染色与玻璃层叠感，统一语义色：交互蓝、AI 紫、成功绿、风险金、错误红。
- Header、Liquid Dock、彩种/分页 Segment、设置入口统一尺寸、圆角和留白。
- Liquid Dock 进一步减重，降低视觉占用并提升内容区域可用空间。
- 首页单 AI 模型时不再突出无意义的 `1/1 票`；多模型场景继续展示共识票数与分布。
- 概率与预测卡压缩纵向密度，在不牺牲可读性的情况下展示更多候补信息。
- 策略页收紧彩种切换、策略/验证导航、号码球、指标卡与实验面板。

## 通知中心

- 减少顶部重复状态与统计信息。
- 通知类型筛选常驻，彩种筛选按需展开。
- 设置面板重新按总开关、彩种、内容类型、高级选项分组。
- 缩小 Switch 行距与卡片留白，提高单屏信息密度。

## 预测档案

- 将易混淆的计数改为更明确的“已结算 / 总计”和“当前加载 / 总计”。
- 搜索和筛选区域进一步紧凑，减少大面积空白。

## AI 对话

- 顶部操作栏进一步减重，仅保留关键操作；历史入口收进更多菜单。
- 模型控制区、空状态和快捷 Prompt 重新压缩，降低首次进入时的视觉负担。
- 提升辅助文字可读性，减少过小字号。

## 服务端控制台

- 通用交互主色统一为蓝色，AI 专属状态继续使用紫色。
- 手机端隐藏重复品牌标题，并压缩卡片、模型项、Profile、开关和工作台留白。
- Realtime 延迟、状态与趋势信息继续保持清晰可读。
- 移动端底部导航移除 `01 / 02 / 03 / 04` 编号。
- 继续强化底部安全区与移动端内容密度，减少遮挡和超高卡片。

## 验证

- Android：版本/UI 契约、Debug/Release 单元测试、Lint、Debug/Release APK 构建、Compose Screenshot 渲染全部通过。
- 服务端：部署脚本校验、Python 编译、单元测试、Docker Compose 校验、Docker 镜像构建全部通过。
- 正式发布流程继续校验应用包名、版本号、APK 签名和与上一正式版相同的升级证书，确保可原位升级。

## 兼容性

- Android applicationId 保持 `com.tianji.probabilitylab.nativev5`。
- 继续使用受保护的正式版签名身份，可从此前正式版本直接覆盖升级。
