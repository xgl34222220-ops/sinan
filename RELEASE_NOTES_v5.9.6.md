# 天机 v5.9.6

这是针对 v5.9.5 底部五项导航显示不完整和云端档案来源混在一起的问题修复版本，不修改预测算法、开奖同步、数据库结构或冻结结算规则。

## 修复

- 修复底部五项导航中中央 `AI` 图标与文字在部分字体、字体缩放和设备上被裁切、只显示一截的问题。
- 底部导航改为自适应最小高度，不再用固定 56dp 强行限制中央 AI 项。
- 为“预测 / 策略 / AI / 档案 / 设置”统一补充明确行高和单行约束，避免自定义字体度量造成错位。
- 适度收紧中央 AI 图标尺寸并补充文字间距，在保持悬浮主操作视觉的同时保证完整显示。
- 档案来源拆分为 `AI 共识`、`手机独立 AI`、`天机云端 AI`、`天机云端本地`、`手机本地模型`。
- `天机云端 AI` 与 `天机云端本地` 使用独立图标、颜色、说明、计数和筛选项，不再混在“独立 AI”中。
- 云端本地来源名称由“天机云端本机”统一为“天机云端本地”。

## 验证

- Debug 与 Release 单元测试通过。
- Android Lint 通过。
- Debug APK 与 Release APK 构建通过。
- AI 对话页面、预测算法、开奖同步和历史数据库未改动。

## 兼容性

- 应用 ID：`com.tianji.probabilitylab.nativev5`
- 版本号：`5.9.6`
- versionCode：`48`
- 延续 nativev5 正式签名，可从 v5.9.5 直接覆盖升级。
