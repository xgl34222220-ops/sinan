# 天机 v6.1.0

这是天机 6.1 系列的正式稳定版本，整合服务端 AI 预测修复、App 界面统一、云端部署状态、通知中心以及 FCM / Telegram 推送改进。

## AI 预测

- 号码评审使用最近 120 期完整历史顺序，并将真实号码随机匿名映射为 A–J。
- 移除近期真实号码和当前号码泄露，避免模型直接照抄最近集合。
- 检测到 Top 7 高度复刻最近七码时，增加隐藏最近 7 期的独立留出评审。
- 最终结果由完整历史评审与留出评审加权汇总，不使用本地统计或随机结果冒充 AI。
- 外部 AI 全部失败时明确记录失败，不生成伪 AI 结果。

## App 界面

- 首页重新突出目标期、预测名次和最终六码。
- 悬浮底栏由统一 Scaffold 管理，正确处理系统安全区域，不再遮挡页面内容。
- 首页、策略、档案和设置页面统一底部间距。
- 首页预测状态改成普通用户可理解的文案：正在生成预测、预测结果已生成。
- 移除“几路 AI 已完成、独立评审、匿名证据”等内部术语。

## 通知与推送

- FCM 改为 data-only，由 App 根据事件类型选择普通通知或风险预警频道。
- 普通预测静默展示，风险预警才使用高优先级提醒。
- 系统通知按彩种归组，并支持“查看预测”和“标记已读”。
- 通知中心支持按未读、预测、预警、恢复、彩种和来源筛选。
- Telegram 消息改为紧凑卡片，并保留提前预警、加强提醒和升级提醒的稳定标识。
- 修复停盘、历史回填和旧队列导致的批量预警轰炸，只允许最新且刚结算的真实开奖生成提醒。

## 云端控制台与部署

- 显示当前运行 Commit、目标 Commit、容器启动时间和最近部署时间。
- 明确显示自动更新成功、检查失败、阻塞、回滚中、已回滚和回滚失败。
- 手动与自动更新都会写入部署状态，便于确认服务器是否真正加载新代码。

## 稳定性与验证

- Android 单元测试、Lint、Debug/Release 构建通过。
- 服务端部署脚本、Python 编译、完整单元测试、Docker Compose 校验和镜像构建通过。
- 保留原应用 ID、签名、数据库和旧版本覆盖安装兼容性。

> 随机开奖不可可靠预测。本项目用于统计实验、记录与真实前向验证，不承诺准确率、收益或必中。
