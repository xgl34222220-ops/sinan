# 天机 v5.9.2

这是一次面向 App、管理控制台与公开云端页面的结构级界面重构。重点不再是继续叠加圆角、渐变和毛玻璃，而是重新整理导航、首屏信息层级、字体可读性和移动端空间利用。

## Android App

- 底部导航重构为“预测、策略、AI、档案、设置”，AI 对话成为中央主入口，不再额外悬浮遮挡内容。
- 顶部栏缩为紧凑原生结构，根据当前页面显示标题和说明。
- 预测首页将最新开奖压缩为一行彩色号码球，并将内容拆分为“概览、概率、模型”三个分段页。
- 首屏集中展示目标期、本机六码、AI 联合分析与冻结状态，减少重复卡片和长页面滚动。
- 策略与时间切分验证合并为“策略与验证”页面，在同一入口查看七码观察、证据闸门和 11 模型成绩。
- 档案页改为 AI 共识、本机模型和独立 AI 的紧凑分组列表，保留哈希、冻结时间和开奖结算状态。
- 设置页拆分为 AI 模型与接口、数据与同步、外观与主题、最近开奖和关于天机等独立子页面。
- 显示模式与强调色正式分离，支持跟随系统、浅色、深色、OLED 纯黑，以及 Monet、跟随彩种、曜金、星紫和青玉强调色。
- 提升正文、标签、状态和辅助说明的字号与行高，减少 6–9sp 的过小文字。
- 保留 v5.9.1 已完成的 AI 对话能力，包括流式生成、历史会话、快捷追问、复制、朗读、重新回答、停止生成和回到底部，并将入口融入主导航。

## 云端管理控制台 Console V6

- 停止同时加载 V3 与 V5 样式，改由一套 Console V6 视觉规则完整接管页面。
- 保留成熟的数据接口以及档案、运维和任务渲染逻辑，不修改服务端业务行为。
- 重做桌面顶栏、侧栏、移动底栏、总览指标、彩种任务、AI 工作台、预测档案、运维状态和设置区域。
- 统一字体等级，提升正文与标签可读性，减少 8px、9px 的关键信息。
- 减少“大白卡套小白卡”、厚重阴影和大面积实时模糊，建立页面、内容区和浮层三级层级。
- 总览与开奖卡压缩信息密度，十个开奖号码保持一行彩色圆球。
- AI 工作台优先展示当前模型和可切换模型，账户及高级管理操作使用更紧凑的结构。
- 档案、模型表现和运维信息采用紧凑网格与列表，降低超长页面的滚动负担。
- 手机底栏降低高度，并支持向下滚动自动隐藏、向上滚动重新出现。

## 公开云端页面 Public V5

- 与 Console V6 统一字体、圆球、表面、阴影和深浅色规范。
- 手机顶部改为轻量运行状态与操作区，减少重复品牌区域。
- 运行指标、彩种状态、AI 任务与冻结预测重新压缩首屏层级。
- 公开页继续使用原有接口、刷新和状态数据，不修改开奖或预测逻辑。

## 数据与兼容性

- 应用 ID 保持 `com.tianji.probabilitylab.nativev5`，沿用既有正式签名升级链。
- 版本升级为 `5.9.2`，`versionCode 44`。
- 不修改预测算法、模型训练、开奖同步、数据库结构、冻结档案、哈希完整性链或结算规则。
- 服务端现有历史数据、AI 配置、管理密码和自动更新配置保持不变。
