# 天机 v5.9.8

本版本将 v5.9.7 的云端三期不中看板升级为 App 可接收、可筛选、可追溯的预测预警系统。预测算法、开奖结算规则和双彩种缓存逻辑保持不变。

## App 预警中心

- 首页顶部新增预警入口和未读数量。
- 设置页新增“预警与推送”。
- 新增完整预警中心，保存最近 120 条记录。
- 展示彩种、预测来源、模型、连续未中期数和最近目标期。
- 点击系统通知可直接打开对应预警详情。
- 支持单条已读、全部已读和手动刷新。

## 独立接收设置

- 可单独开关幸运飞艇和澳洲幸运10。
- 可单独开关天机云端 AI 和天机云端本地。
- 可关闭连续 4、5 期仍不中时的升级提醒。
- 所有设置会同步到云端，设备之间互不影响。

## 云端推送与防重复

- 新增设备注册、身份密钥和 FCM 令牌接口。
- 按“彩种 + 来源 + 模型 + 连错期数 + 最新目标期”生成唯一预警事件。
- 同一设备同一事件只发送一次，避免每次刷新重复推送。
- 达到连续 3 期时发送首次预警；连续 4、5 期仍不中时可发送升级预警。
- 命中后连错状态由原有结算逻辑自动清零，后续重新达到 3 期才再次触发。
- 设备令牌失效时自动停用，失败发送按间隔重试。
- 即使 FCM 尚未配置，App 仍会通过 Android 后台任务约每 15 分钟同步预警中心并弹出本地通知。

## 安全与配置

- Firebase 客户端参数通过构建配置注入，不写入源码。
- Firebase 服务账号仅从服务端环境变量读取，不写入仓库。
- 每台设备使用独立安装标识和随机密钥访问预警接口。
- 设备密钥使用 SHA-256 保存，并通过恒定时间比较验证。

## 验证

- Debug 与 Release 单元测试通过。
- Android Lint 通过。
- Debug APK 与 Release APK 构建通过。
- 服务端全部测试通过。
- 专项测试覆盖设备身份、独立接收设置、事件防重复、升级预警和逐设备已读状态。
- 正式 APK 继续使用 nativev5 发布签名，并与 v5.9.7 已发布 APK 校验证书一致性。

## 兼容性

- 应用 ID：`com.tianji.probabilitylab.nativev5`
- 版本号：`5.9.8`
- versionCode：`50`
- 可从 v5.9.7 直接覆盖升级。
