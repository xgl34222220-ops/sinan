name: Patch Tianji AI independence v5.7.1

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "AI independence source patch already applied"
