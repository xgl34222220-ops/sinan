name: Patch server AI raw history

on:
  push:
    branches: ['fix/server-ai-position-collapse-v2']

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[server-ai-raw-history-patched]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/server-ai-position-collapse-v2
          fetch-depth: 0
      - name: Apply anonymous raw-history patch
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/patch_server_ai_raw_history.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server/app/ai_ensemble.py
          git commit -m 'fix(server): feed AI anonymous raw API history [server-ai-raw-history-patched]'
          git push origin HEAD:fix/server-ai-position-collapse-v2
