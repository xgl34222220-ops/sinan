# 天机 v6.7.0

v6.7.0 聚焦 **双彩种实时同步、Dynamic AI v2 展示一致性和移动端 UI 收敛**。本版本把“开奖实时状态”和“完整预测计算”拆成不同通道，让幸运飞艇与澳洲幸运10持续同步，同时避免每次轮询都重复执行历史、结算和预测重计算。

## 双彩种实时同步

- 服务端新增轻量只读接口 `/v1/public/realtime`，一次返回两个彩种的最新期号、开奖号码、下一期、下期开奖时间和同步时间。
- Android 使用同一请求维持双彩种实时状态；临期开奖窗口提高轮询频率，远离开奖时自动降低请求频率。
- 只有检测到实际期号变化或达到兜底周期时，才触发完整历史同步、结算和预测刷新。
- 手动刷新、Push 触发刷新和云端失败回退统一同步两个彩种，不再只处理当前选中的彩种。
- AI 正式预测与 AI 对话运行期间不再暂停开奖同步；旧目标期结果仍由现有 target/deadline guard 丢弃。

## AI v2 展示与来源隔离

- Android AI lane 只接受明确 `source=ai` 的云端预测，云端 `native` 结果不会再混入 AI 共识。
- 云端 AI 与手机 AI 分开计数、分开展示，避免用户误判预测来源。
- 首页直接展示 AI v2 动态 Top6 / Top7、与本地模型重合数量、近 20 / 50 期真实结算表现和逐号码概率对比。
- 多路 AI 聚合先确定共识名次，再只聚合同名次结果，避免跨名次概率平均造成语义错位。
- 保留 Dynamic AI v2 的真实前向结算、冻结历史、匿名 reviewer 和独立学习命名空间。

## UI 与适配

- 首页改为“两个彩种实时状态 → 当前开奖 → 本地模型 / AI v2 → 概率或模型表现”的稳定层级。
- 手机采用纵向预测结构；可用宽度达到 600dp 后，本地模型与 AI v2 自动进入双栏。
- 设置入口、背景装饰和普通信息卡片继续减重，只对预测、异常和可操作信息保留较强视觉层级。
- 去除页面级额外字体放大，统一遵循系统字体缩放。
- 新增 412dp 手机和 840dp 平板 Compose Screenshot 基线并完成实际视觉检查。

## 服务端与回归保护

- `/v1/public/realtime` 有独立服务端 unittest，确保两个彩种同时返回且字段语义稳定。
- Android 增加双彩种实时解析与 AI 来源隔离回归测试。
- 旧的“只刷新当前彩种 / 必须使用 V62 首页”契约已更新为 v6.7 双彩种与新版首页语义。
- Server CI 已通过 Python 编译、完整 unittest、Docker Compose 校验和后端镜像构建。
- Android CI 已通过 Unit Test、Lint、Debug / Release Build、Compose Screenshot 渲染和产物上传。
- 正式应用 ID 保持 `com.tianji.probabilitylab.nativev5`，`versionCode` 升级为 72，可直接覆盖 v6.6.0。

> 天机用于统计实验与真实前向验证。随机开奖不可可靠预测，本版本不承诺准确率、收益或必中。
