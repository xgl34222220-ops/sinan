# 天机 v6.1.0-beta01

本次为稳定化预发行版，重点统一 App、服务端和推送端的事件协议，减少重复通知、错误标题和长期运行积压。

## App

- 推送协议升级到 Push Protocol v2。
- 预测完成、恢复命中、两期预警、三期加强和后续升级使用统一解析。
- 直接采用服务端标题和正文，两期预警不再误显示为三期。
- 预警改为增量同步，避免每次重新读取全部历史。
- 设备密钥迁移到 Android Keystore AES/GCM。
- 拆分“预测与恢复”和“风险预警”两个系统通知频道。
- 同一模型的两期、三期及后续升级会更新同一条通知。
- 新增专用单色状态栏图标。
- 通知权限改为进入通知中心时再申请。
- 通知中心按事件类型显示不同图标、颜色、标题和正文。

## 服务端与推送

- 新增可重复执行的 SQLite 数据库迁移体系。
- 增加事件类型、严重程度、折叠键、深链和过期时间。
- Telegram 预测完成和恢复命中事件同步进入 App/FCM 事件流。
- FCM 使用受限并发投递，并增加过期过滤与失败重试窗口。
- 无效 FCM Token 自动清理。
- “全部已读”改为设备已读游标，避免历史逐条写入。
- 自动清理旧投递记录、过期事件和长期失活设备。
- Worker 周期超过硬超时时间后退出，由 Docker 自动拉起干净进程。

## 构建与验证

- Android 单元测试、Lint、Debug/Release 构建纳入 CI。
- 服务端迁移幂等、协议规范化和原有 64 项测试纳入 CI。
- GitHub Actions 升级到 Node 24 兼容版本。
- Beta、Alpha 和 RC 版本自动发布为 GitHub 预发行版。

## 测试重点

这是预发行版本，请重点检查：

- 首次打开通知中心后的权限流程；
- App 前台、后台和被划掉后的 FCM 行为；
- 两期预警升级到三期、四期时是否更新原通知；
- 预测完成和恢复命中在通知中心中的展示；
- 旧版本数据库升级后的档案、推送设置和历史记录。

> 随机开奖不可可靠预测。本项目用于统计实验、记录与真实前向验证，不承诺准确率、收益或必中。
