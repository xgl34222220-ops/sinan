# 天机 v6.9.2

v6.9.2 聚焦云端 AI 调度与控制台体验：两个彩种真正独立并行、每彩种可单独关闭 AI、模型卡片统一，并将发布硬截止由 60 秒调整为 40 秒。

## AI 调度

- 影音先锋艇与澳洲幸运10各使用独立单线程 AI executor，互不抢占 worker；同一个彩种仍保持串行，避免同一期重复消费。
- 服务端原有 run_all_cycles 双彩种并行保持不变，新的独立 AI executor 进一步隔离上一轮慢请求造成的跨彩种排队。
- AI 发布硬截止调整为开奖前 40 秒；目标期变化、目标期已开奖或已有前向记录时仍拒绝写入。

## 彩种独立开关

- AI 工作台保留全局总开关。
- 新增“幸运飞艇 AI”和“澳洲幸运10 AI”独立开关，默认均开启，兼容旧部署。
- 关闭某彩种只停止其服务端 AI 请求；开奖同步、本机云端统计/预测、结算、推送与历史档案继续运行。
- 独立开关持久化在云端数据库，不改变现有 API Key、接口、模型账户格式。

## 控制台 UI

- 当前 Key 读取到的所有模型统一为两列等高模型卡。
- 不再区分“前两个大卡 + 其余小胶囊”，当前模型继续使用高亮状态。
- 总览与运行诊断会明确显示单个彩种 AI 已关闭，避免把旧任务状态误认为仍在排队。

## 版本

- versionName：`6.9.2`
- versionCode：`77`
- 正式应用 ID 与既有签名链保持不变，可直接覆盖升级 v6.9.1。

> 天机用于统计实验、记录与真实前向验证。随机开奖不可可靠预测，本项目不承诺准确率、收益或必中。第三方 AI 网关的并发、速率、额度与计费由服务商决定。
