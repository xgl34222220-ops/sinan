# 天机 v6.9.1

v6.9.1 是 v6.9.0 的 AI 接口兼容与云端模型切换修复版，重点解决自定义 / 中转接口地址兼容、错误模型恢复，以及云端控制台错误展示固定 DeepSeek 快捷模型的问题。

## AI 兼容接口恢复

- 自定义 OpenAI 兼容接口支持只填写域名或 `/v1`，天机会按当前协议自动补全常用 Chat Completions / Responses 路径，减少因路径不完整导致的 404。
- 接口返回模型不支持、400 / 422 等错误时，会尝试从错误信息中提取服务端明确给出的可用模型。
- 可用模型会直接显示为可点击候选项，选择后自动重新测试当前配置，减少手工复制模型 ID 和重复测试。
- 当前 AI 测试失败后不再继续作为可启用配置；存在其它已验证可用配置时自动回退，避免正式预测继续请求失效接口。

## 云端模型切换修复

- 移除云端 AI 工作台中写死的 `DeepSeek V4 Flash / Pro` 快捷按钮。
- 未读取当前 Key 支持的模型前，控制台只显示“待读取模型”，不再暗示某个供应商模型可用。
- 点击“读取此 Key 支持的模型”后，仅根据当前接口和当前 Key 实际返回的模型生成快捷切换项。
- FreeModel、OpenAI 兼容中转、DeepSeek 等不同服务互不串模型；同一个 Key 下仍可直接切换已确认支持的模型，无需重新输入 Key。
- 更新服务端 UI 契约测试，明确禁止通用控制台重新出现固定 DeepSeek 快捷模型。

## 版本与兼容性

- versionName：`6.9.1`
- versionCode：`76`
- 保持正式应用 ID `com.tianji.probabilitylab.nativev5` 与既有正式签名证书，可直接覆盖升级 v6.9.0。
- 不改变预测结果冻结、开奖同步、结算、历史档案与推送语义。

> 天机用于统计实验、记录与真实前向验证。随机开奖不可可靠预测，本项目不承诺准确率、收益或必中。第三方 AI 网关的可用性、额度、模型列表与计费规则由对应服务商决定。
