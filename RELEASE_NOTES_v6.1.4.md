# 天机 v6.1.4

本次更新重点优化开奖后的整条实时链路：最新期开奖发现、结算、推送、管理台展示和 App 刷新不再互相串行阻塞。同时保留 v6.1.3 之后固定 `235780` 的结算修复与 AI 输出保护，本地 native 动态预测逻辑保持不变。

## 实时开奖链路

- 服务端新增独立实时开奖 Worker，最新开奖探测与历史补齐、AI 任务、Telegram/FCM 网络请求拆开执行。
- 开奖窗口采用约 2 秒快速探测，临近开奖自动提频，平时降频，减少无效请求。
- 新期开奖先写入数据库并结算，随后异步触发通知与完整周期，降低上游已出结果后的额外等待。
- 新增开奖探测耗时、结算耗时、检测延迟、EMA 与最大延迟状态，便于区分上游 API 晚出和天机自身处理慢。

## App 快刷新

- App 每次优先读取最新开奖；完整历史已校验后，普通短刷新直接复用已验证历史缓存。
- `nextIssue` / `drawIssue` 语义与服务端统一，目标期必须严格晚于最新已开奖期，减少倒计时或期号错一位的风险。
- 前台开奖窗口约 3 秒刷新、临近约 8 秒、平时 30 秒，并且只刷新当前正在查看的彩种。
- 收到预测完成、连续不中、升级预警或恢复命中等推送后，会主动触发当前彩种快刷新，不再只显示系统通知。
- 云端档案请求缩短超时并使用缓存兜底，避免云端附加数据拖慢首页最新期开奖展示。

## 推送与管理台

- 推送链路收口为单一正式实现，移除多层运行时 monkey patch 叠加造成的实际执行路径漂移。
- FCM 与 Telegram 在独立线程池中异步并行投递，慢网络不再阻塞开奖主循环。
- FCM 继续使用 data-only 消息，由 App 自己负责通知频道、去重与深链行为。
- 管理台开奖卡片使用 3/8/30 秒自适应轻量刷新，只更新期号与倒计时，不再频繁重载整个控制台状态。

## 固定 235780 兼容保护

- 保留并整合 v6.1.3 之后的固定 `2/3/5/7/8/10` AI 结算修复，旧动态 AI 档案会按固定集合重新归一。
- AI 正式预测在写入边界强制输出固定 `235780`；本地 native 动态预测不受此约束。
- 位置模型继续使用更保守的长窗口与严格前向验证，不使用“连续不中后应反弹”的加分逻辑。

## 验证

本版本在合并前通过服务端 Python 编译、完整单元测试、Docker Compose 校验、后端镜像构建，以及 Android Debug/Release 单测、Lint、Debug APK 与 R8 Release APK 构建。

固定 `235780` 对任意固定名次的随机命中基准仍为 60%。本版本优化的是数据链路、结算正确性、实时性和工程稳定性，不代表随机开奖可以被可靠预测，也不保证收益或必中。
