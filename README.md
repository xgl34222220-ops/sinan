# 天机 · 开奖概率实验室

天机是一款面向历史开奖数据分析、候选预测和真实前向验证的 Android 应用。
应用从开奖接口同步历史与目标期开奖数据，本机统计模型和可选 AI 分析都基于同一份接口历史进行计算。

## 当前版本

- 版本：5.5.6
- Android：原生 Kotlin + Jetpack Compose
- 构建：Java 17、Gradle 8.13、Android SDK 36
- 签名：正式版使用稳定 Release 证书

## v5.5.6 分析对话、具体模型与历史归档

- 对话页将“API 配置”和“具体模型”分开选择，不再把配置名称当成模型名称。
- DeepSeek 配置默认可选 `deepseek-v4-flash` 与 `deepseek-v4-pro`，同时显示接口拉取到的其他可用模型。
- 切换具体模型会进入该模型自己的当前目标期会话，不覆盖其他模型的聊天和候选。
- 每个“彩种 + 目标期 + AI配置 + 具体模型”独立归档；开奖后自动进入新目标期，上一期对话、候选、人设和所用模型继续保留。
- 顶部历史入口可查看旧目标期和旧模型的只读记录，并可返回当前目标期。
- 对话归档写入应用私有目录并使用原子写入；关闭应用、进程回收或重启后仍可恢复。
- 历史对话候选与正式预测数据库隔离，不写入真实成绩、不替换首次冻结预测、不参与正式共识。
- 对话支持 Chat Completions SSE 与 Responses API 流式输出；接口不支持流式时自动降级并平滑分段显示。
- 内置综合研判、历史分析、走势研判、遗漏追踪、转移分析、风险审计六种分析人设。

## 正式预测提速

- 正式 AI 只返回 `position` 与 10 项 `scores`，六码、七码、概率矩阵和中文核验摘要由客户端生成。
- 保留模型自身最大 Token 空间、真实 thinking 和完整 60/120 期历史。
- 流式响应一旦出现有效 position 与 10 项 scores，立即结束多余输出并进入本机校验。
- 修复流式 `content=null` / `reasoning_content=null` 被错误拼接成 `nullnull…` 的问题。
- 普通超时、断流、空响应或无效 JSON 明确失败，不隐式启动第二轮完整预测。

## 自动验证

- Debug 单元测试：48 项，0 失败，0 跳过。
- Release 单元测试：48 项，0 失败，0 跳过。
- Android Lint：No Issues Found。
- Debug APK 与 R8 Release APK 均构建成功。

> 随机开奖不可可靠预测。本项目用于统计实验、记录和真实前向验证，不承诺准确率、收益或必中。
