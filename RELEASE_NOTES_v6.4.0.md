# 天机 v6.4.0

v6.4.0 是一次面向正式体验收口的 UI Final Polish：重点提升首页信息层级、AI 共识可读性、移动端交互一致性、档案与通知浏览效率，以及服务端控制台的手机端可读性。

## Android UI / 体验

- 首页正常开奖源状态进一步降噪：数据健康时只保留轻量同步状态，缓存、超时或错误时才展开详细提示。
- AI 联合判断新增共识强度与票数分布，可直接区分“高度一致 / 主判断明确 / 存在分歧”，并以短条展示主要位置的模型支持度。
- AI 摘要默认只保留少量模型明细，其余结果引导进入 AI 对话查看，避免首页卡片过长。
- 概率页继续强化六码边界，第 6 / 第 7 名分界更易识别。
- 顶部 Header 进一步收紧，刷新改为自然旋转反馈，保持原生且不抢占内容空间。
- 手机 Bottom Dock 改为真正的滑动 Liquid 选中胶囊，首页 / 策略 / 档案 / 设置切换更连贯；AI 中心入口保持独立悬浮形态。

## 通知中心

- FCM 即时推送正常时不再重复展示完整连接状态卡，只在降级、未注册或异常状态下重点提示。
- 筛选状态进一步明确，并补充快速清除筛选能力。
- 预测、风险、恢复等通知继续保留明确的动作去向与整卡点击导航。

## 预测档案

- 增加今天 / 昨天 / 日期分组，长列表浏览更清晰。
- Sticky 搜索与筛选区继续保留，并显示当前筛选数量与清除入口。
- 工程 Hash 不再长期占用普通档案卡片视觉空间，默认界面只保留用户真正需要的来源、目标期、位置、六码、结算状态和时间。
- 渐进加载逻辑保持不变，不再静默截断历史记录。

## 设置与 AI 对话

- 设置入口加入轻量语义色，AI / 数据 / 推送 / 历史等入口更容易快速识别，同时保持统一玻璃卡片体系。
- 删除旧的“系统字体再乘倍率”方案，新增最低可读字体比例机制：默认字体下适度托底，系统本身使用大字体时完全尊重系统设置，不再二次放大。
- AI 对话与设置页共同采用可读性托底，修复模型标签和辅助信息过小的问题。

## 服务端控制台

- 手机端继续收紧信息密度，同时把状态、延迟、趋势和底栏文字的可读下限提升到约 11px。
- 统一实时开奖卡片、控件和状态区域的 v6.3/v6.4 视觉 token，减少历史 CSS 覆盖造成的圆角、间距和字号漂移。
- 运行总览、异常彩种优先、开奖发现 / 单次探测 / 写库结算三段延迟结构保持不变。

## 回归与工程质量

- 新增 360dp 小屏、浅色首页、档案 populated 状态、推送降级状态、设置语义配色等 Compose Screenshot 场景。
- Android 版本/UI 契约、Debug / Release 单元测试、Lint、两套 APK 构建与 Compose Screenshot 已在功能分支通过。
- 服务端 Python 编译、单元测试、Docker Compose 校验和 Docker 镜像构建已通过。

## 安装与升级

- 应用 ID 保持 `com.tianji.probabilitylab.nativev5`。
- versionCode 升级为 `69`，versionName 为 `6.4.0`。
- 正式 Release 使用与 v6.3.0 相同的受保护签名身份，可直接覆盖升级。
- 发布工作流会再次校验上一正式版 APK 签名、当前 APK 签名、包名、versionCode、versionName 与 SHA-256 后再发布。
