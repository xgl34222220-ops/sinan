# 天机 v6.1.0-beta02

本次为服务端 AI 预测修复预发行版，重点解决号码阶段过度复刻所选名次最近七期号码集合的问题，同时保留完整历史时序分析能力。

## 服务端 AI 预测

- 号码评审继续读取所选名次最近 120 期完整历史先后顺序。
- 每轮评审都会将号码 1–10 随机映射为 A–J，AI 只能看到匿名时序。
- 频率、遗漏、趋势和状态转移证据与匿名历史使用同一套映射。
- 移除会暴露真实号码身份的近期真实号码序列和当前号码字段。
- AI 仍可分析连续走势、重复间隔、冷热切换、状态转移和多窗口稳定性。
- 检测到 Top 7 与最近七期候选集合高度重合时，增加隐藏最近七期的独立留出 AI 评审。
- 最终结果由完整历史评审与留出评审加权汇总，不由程序强制删除、替换或随机补入号码。
- 外部 AI 全部调用失败时直接记录失败，不使用本地统计或随机结果冒充 AI。

## 验证

- 新增完整匿名历史顺序测试。
- 新增真实号码映射泄露测试。
- 新增近期七码集合复刻检测与留出复核测试。
- 新增完整历史与留出评审加权测试。
- 服务端编译、单元测试、Compose 配置和 Docker 镜像构建全部通过。
- Android 单元测试、Lint 和构建验证通过；本次没有修改客户端业务逻辑。

## 说明

- 本版本发布后，只有部署或更新到该版本的服务端才会使用新的 AI 预测逻辑。
- 已经冻结的历史预测记录不会被重新计算或覆盖。

> 随机开奖不可可靠预测。本项目用于统计实验、记录与真实前向验证，不承诺准确率、收益或必中。
