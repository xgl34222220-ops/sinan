# 天机 v5.10.3

## AI 预测目标期修复

- 修复对话页顶部显示当前目标期，但 AI 正文仍复制旧会话期号的问题。
- 当前目标期在每次预测请求开始时锁定，并在模型提示末尾再次明确，旧期号只作为历史复盘内容。
- 历史对话会标注其原目标期，避免模型把上一期预测标题误当成本期。
- 对最终预测标题和“目标期”字段增加客户端一致性校验；不会改写历史样本、截至期或复盘证据中的期号。
- 候选卡、消息记录和开奖结算继续使用同一份当前目标期。

## 验证

- 增加普通标题、Markdown 标题、目标期字段和历史证据不被误改的单元测试。
- 执行 Android 单元测试、Lint、Debug 与 Release 构建。
