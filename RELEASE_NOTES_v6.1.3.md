# 天机 v6.1.3

本次更新把预测目标彻底统一为固定 `235780`，不再让 AI 或本机模型动态生成六码。系统现在只比较第 1～10 名中，哪个位置下一期更可能落入固定集合 `2/3/5/7/8/10`（界面玩法中的 0 以 10 表示）。

## 固定 235780 位置预测

- App、服务端本机模型和云端 AI 使用同一个固定目标：`235780`。
- 十个名次分别建立二分类概率与滚动前向验证，随机命中基准明确为 60%。
- 最终输出只保留“第 X 名 + 固定 235780”，避免动态六码让预测目标漂移。
- 前向证据按近期与中期窗口计算，并记录命中率、Binary Log Loss、当前/最长连续未进等指标。

## AI 自学习

- 远端 AI 推理前会收到该 AI 自己已经结算的真实历史表现和十名次滚动前向证据。
- 自学习证据按 AI 配置、模型和具体名次隔离，不混用不同模型的成绩。
- 不向远端 AI 发送本机模型最终名次、六码、七码或概率矩阵，保持两套预测来源独立。
- AI 返回后再与前向证据融合，避免一次极端语言模型评分直接覆盖长期真实成绩。

## 控制台与推送

- 移除控制台里冗长的双彩种不中卡片和开发阶段/毫秒耗时列表，日常页只保留服务、AI、App 预警和 Telegram 状态。
- 后台连续不中统计与预警逻辑仍然保留，不因控制台隐藏而停止。
- Telegram 预警链路调整为结算后优先发送，普通预测队列靠后处理。
- Telegram 网络超时缩短，失败重试冷却缩短；超过 8 分钟的普通预测消息自动抑制，避免迟到补发。

## 说明

固定 `235780` 在每期十个号码中覆盖六个号码，因此任意固定名次的随机命中基准就是 60%。本版本重点是让预测目标稳定、可验证、可比较，并不代表随机开奖可以被可靠预测，也不保证收益或必中。