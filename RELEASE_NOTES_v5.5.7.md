# 天机 v5.5.7 正式版

本版集中修复正式预测耗时过长，以及分析对话关闭后请求被取消的问题。

## 正式预测速度

- DeepSeek V4 不再使用 384K 无边界输出预算。
- 自动模式使用 8K、省时模式使用 4K、深入模式使用 32K 的明确输出上限。
- 自动与省时模式直接使用客户端已核验的 60/120 期统计生成紧凑 JSON 评分矩阵，避免数分钟长思考。
- 深入模式仍保留完整 DeepSeek thinking，供主动选择高强度分析时使用。
- 历史接口同步、目标期二次核验、首次冻结、概率矩阵校验和真实成绩验证保持不变。

## 分析对话后台运行

- 关闭、返回或收起分析对话只隐藏界面，不再自动调用取消。
- 请求继续由应用级控制器运行；重新打开后可继续查看流式内容或最终结果。
- “停止生成”按钮仍会立即取消当前请求。

## 验证

- 新增正式预测与聊天推理策略隔离测试。
- 新增 DeepSeek 自动/省时/深入输出预算测试。
- 通过 Release 单元测试、Android Lint 与 R8 Release APK 构建后发布。
